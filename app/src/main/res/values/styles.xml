<?xml version="1.0" encoding="utf-8"?>
<resources>

<!-- Base application theme. -->
<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
</style>

<!-- Your custom AlertDialog theme -->
<style name="AlertDialogCustom" parent="Theme.AppCompat.Dialog.Alert">
    <!-- Background color -->
    <item name="android:background">#A0327D68</item>

    <!-- Title and message color -->
    <item name="android:textColorPrimary">@color/white</item>

    <!-- Button colors -->
    <item name="colorAccent">@color/white</item>
</style>

    <style name="AlertDialogProducts" parent="Theme.AppCompat.Dialog.Alert">
        <!-- Background color -->
        <item name="android:background">@drawable/gradient_green_main</item>

        <!-- Title and message color -->
        <item name="android:textColorPrimary">@color/white</item>

        <!-- Button colors -->
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="MyTimePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#1e5948</item>
        <item name="android:textColorPrimary">#1e5948</item>
        <item name="android:textColorSecondary">#A0327D68</item>
        <item name="android:windowBackground">#A0327D68</item>
    </style>



</resources>